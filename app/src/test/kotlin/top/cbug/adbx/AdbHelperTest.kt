package top.cbug.adbx

import org.junit.Test
import org.junit.Assert.*

/**
 * auto-fix: scaffolded unit test for AdbHelper.
 * Replace these placeholder tests with real ones as you go.
 *
 * Requires JUnit 4 in app/build.gradle.kts:
 *   testImplementation("junit:junit:4.13.2")
 */
class AdbHelperTest {

    @Test
    fun placeholder_assertion_works() {
        // Replace with a real AdbHelper unit test.
        // Note: AdbHelper depends on Android Context, so most tests
        // will need Robolectric or refactoring to make logic testable.
        assertEquals(2 + 2, 4)
    }
}
