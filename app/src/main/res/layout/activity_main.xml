<?xml version="1.0" encoding="utf-8"?>
<!--
    Root is a vertical LinearLayout: fragment takes the remaining height,
    BottomNavigationView sits pinned to the bottom. No toolbar / appbar —
    the bottom-nav tabs themselves are the primary navigation surface
    and the app name doesn't need a separate header.
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="?attr/colorSurface">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:menu="@menu/bottom_nav"
        app:labelVisibilityMode="labeled"
        app:elevation="8dp" />

</LinearLayout>